# IPOS Runtime

The initial runtime target is a tightly controlled Chromium-based web runtime.

Each web application must have an explicit identity, storage boundary, permission set and network policy.
